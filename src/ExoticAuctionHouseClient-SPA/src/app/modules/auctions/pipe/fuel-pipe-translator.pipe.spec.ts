import { FuelPipeTranslatorPipe } from './fuel-pipe-translator.pipe';

describe('FuelPipeTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new FuelPipeTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
