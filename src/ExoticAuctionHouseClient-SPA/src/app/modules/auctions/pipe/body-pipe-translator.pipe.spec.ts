import { BodyPipeTranslatorPipe } from './body-pipe-translator.pipe';

describe('BodyPipeTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new BodyPipeTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
